<%= simple_form_for [event, registration] do |f| %>
  <%= f.input :introduction, placeholder: 'Dites en plus ici', label:false,  as: :text, input_html: { data: { }, class: "input-field" } %>
  <%= f.button :submit, "Rejoindre l'événement", class: "button-primary-register a-style"  %>
<% end %>
